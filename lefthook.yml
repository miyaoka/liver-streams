pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,ts,vue}"
      run: pnpm eslint --cache --fix {staged_files}
      stage_fixed: true
    prettier:
      glob: "*.{js,ts,vue,css,scss,md}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
